# 학습 정리 — 환율 AI 비서 과제

이 문서는 과제의 "학습 목표" 6가지를 이번 프로젝트의 실제 코드를 근거로 정리한 것입니다.

---

## 1. 시계열 데이터 분석 → 요약 정보 → 서비스 활용 흐름

**흐름**: 원본 데이터 수집 → Firestore 저장 → 요약 계산 → API로 노출 → AI 채팅에 활용

1. `seed_data.py`가 Frankfurter API에서 최근 200영업일치 USD→KRW 환율을 가져와 `(date, value, memo)` 형태로 Firestore `data` 컬렉션에 저장한다.
2. `data.py`의 `get_summary()`가 이 원본 데이터 전체를 `stream()`으로 읽어와서, 파이썬 레벨에서 통계(개수, 기간, 평균/최고/최저, 최근 두 값 비교로 추세 판단)를 계산한다.
3. 이 계산 결과는 `GET /api/data/summary` 엔드포인트로 외부에 노출된다.
4. `chat.py`가 이 요약을 그대로 가져와 AI에게 전달할 문장으로 가공한다.

**핵심**: 원본 데이터(200개 row)를 AI에게 그대로 주지 않고, **먼저 의미 있는 통계로 압축(요약)한 뒤** 활용한다는 점이 이 흐름의 핵심이다. 원본을 통째로 넘기면 토큰 낭비도 크고, 모델이 스스로 통계를 계산하다 실수할 위험도 있다.

---

## 2. FastAPI 프로젝트를 라우터/서비스로 분리한 기준
backend/
├── main.py ← 앱 초기화, 미들웨어(CORS), 라우터 등록만 담당
├── firebase_config.py ← 외부 인프라(Firestore) 연결 담당
└── routers/
├── data.py ← "데이터" 도메인의 엔드포인트 + 로직
├── conversations.py ← "대화 기록" 도메인의 엔드포인트 + 로직
└── chat.py ← "AI 챗봇" 도메인의 엔드포인트 + 로직

**분리 기준**: **"이 코드가 왜 존재하는가"** 를 기준으로 나눴다.

- `main.py`는 애플리케이션 자체의 설정(어떤 라우터를 쓸지, CORS를 어떻게 열지)만 알면 되고, 각 도메인의 세부 로직은 몰라도 된다.
- `firebase_config.py`는 "Firestore에 어떻게 연결하는가"라는 **인프라 관심사**만 다루고, 어떤 컬렉션을 어떻게 쓰는지는 모른다. 그래서 `data.py`, `conversations.py`, `chat.py`가 모두 이 함수 하나(`get_firestore_client()`)를 공유해서 쓸 수 있다.
- `routers/` 안의 각 파일은 하나의 **도메인(리소스) 단위**로 나뉜다. `data`와 `conversations`는 서로의 존재를 몰라도 되지만, `chat.py`만 예외적으로 `data.py`의 `get_summary()` 함수를 직접 import해서 재사용한다 — 이건 "챗봇이 데이터 요약에 의존한다"는 실제 관계를 코드 구조에 그대로 반영한 것이다.

이렇게 나누면 각 파일이 하나의 책임만 지므로(단일 책임 원칙), 예를 들어 대화 기록 저장 방식을 바꿔야 할 때 `conversations.py`만 보면 되고 `data.py`는 건드릴 필요가 없다.

---

## 3. Pydantic으로 요청 데이터 검증을 적용한 이유와 방식

**이유**: FastAPI는 클라이언트가 어떤 데이터를 보낼지 신뢰할 수 없다. 타입이 안 맞거나 필수 필드가 빠진 요청이 그대로 Firestore까지 넘어가면, 저장 시점이나 나중에 조회할 때 예상치 못한 에러가 난다. Pydantic은 **요청이 라우터 함수에 도달하기도 전에** 자동으로 검증하고, 틀리면 422 에러를 자동으로 돌려준다.

**방식** (실제 사용 예):

```python
class ExchangeRateItem(BaseModel):
    date: date_type          # 날짜 형식이 아니면 자동으로 거부
    value: float              # 숫자가 아니면 자동으로 거부
    memo: Optional[str] = None  # 없어도 되는 필드는 기본값 지정
```

`POST /api/data`에서 `item: ExchangeRateItem`처럼 타입 힌트로 선언만 하면, FastAPI가 요청 body를 이 모델에 맞춰 자동으로 파싱·검증한다. 검증을 통과한 뒤에는 `item.model_dump()`로 안전하게 dict로 변환해서 Firestore에 저장한다.

같은 원리로 `chat.py`의 `ChatRequest`, `conversations.py`의 `ConversationCreate`/`Message`도 모두 "이 엔드포인트가 정확히 어떤 형태의 데이터를 기대하는지"를 코드로 문서화하는 역할을 겸한다 — 이게 Swagger UI에 스키마가 자동으로 표시되는 이유이기도 하다.

---

## 4. Firestore에 데이터를 저장하고 CRUD로 다루는 방법

Firestore는 컬렉션(테이블과 비슷) 안에 문서(row와 비슷, 고유 id를 가짐)를 저장하는 NoSQL 문서 DB다.

| 동작 | 코드 | 설명 |
|---|---|---|
| Create | `db.collection("data").document().set(data)` | 새 문서를 자동 id로 생성 |
| Read (전체) | `db.collection("data").stream()` | 컬렉션의 모든 문서를 순회 |
| Read (단건) | `db.collection("data").document(id).get()` | id로 문서 하나 조회, `.exists`로 존재 확인 |
| Update | `db.collection("data").document(id).update(data)` | 기존 문서의 필드만 갱신 |
| Delete | `db.collection("data").document(id).delete()` | 문서 삭제 |

**패턴**: 수정/삭제 전에는 항상 먼저 `.get().exists`로 존재를 확인하고, 없으면 `HTTPException(404)`를 던진다. 이렇게 하지 않으면 존재하지 않는 id에 대한 요청이 조용히 아무 일도 안 하고 200을 반환해버려서, 클라이언트 입장에서 성공인지 실패인지 알 수 없게 된다.

---

## 5. 컨텍스트 주입(Context Injection)의 원리

**전제**: LLM(Gemini, GPT 등)은 기본적으로 **stateless**하고, 우리 서비스의 Firestore에 어떤 데이터가 있는지 전혀 모른다. 학습 시점에 존재하지 않았던 정보이기 때문이다.

**원리**: 매 요청마다 모델에게 필요한 정보를 **자연어 문장으로 만들어 프롬프트 안에 직접 끼워 넣는다.**

```python
def build_system_prompt(summary: dict) -> str:
    return (
        "당신은 사용자의 USD 환율 데이터를 알고 있는 AI 비서입니다. "
        f"- 데이터 개수: {summary['count']}개\n"
        f"- 평균: {summary['average']}\n"
        ...
    )
```

이 문자열이 `GenerateContentConfig(system_instruction=...)`로 모델에게 전달되면, 모델은 마치 원래 그 사실을 "알고 있었던 것"처럼 답변에 활용한다. 실제로는 모델의 가중치에 저장된 지식이 아니라, **이번 요청의 프롬프트에 임시로 주입된 컨텍스트**일 뿐이다.

**주의점**: 이 방식은 데이터 범위를 벗어난 질문에도 모델이 그럴듯하게 지어낼(hallucination) 위험이 있다. 그래서 시스템 프롬프트 끝에 "위 데이터 범위를 벗어나는 질문에는 모른다고 솔직히 답하세요"라는 지시를 명시적으로 넣어 이를 방지했다.

---

## 6. 배포 환경에서 CORS / 환경변수 / 키 관리가 필요한 이유

**CORS (Cross-Origin Resource Sharing)**
프론트엔드(`https://m1-2-steel.vercel.app`)와 백엔드(`https://m1-2-jmkr.onrender.com`)는 **도메인이 서로 다르다.** 브라우저는 보안을 위해 기본적으로 "다른 출처(origin)로의 요청"을 차단하는데(동일 출처 정책), 백엔드가 `CORSMiddleware`로 "이 출처의 요청은 허용한다"고 명시적으로 선언해줘야 브라우저가 fetch 요청을 통과시킨다. 로컬 개발 중에는 `allow_origins=["*"]`로 전부 허용해도 되지만, 실서비스에서는 실제 프론트 도메인만 허용하는 게 안전하다.

**환경변수**
같은 코드(`firebase_config.py`)가 로컬(파일 경로)과 Render(JSON 문자열)에서 **서로 다른 방식으로 값을 읽어야 했다.** 이걸 코드에 하드코딩하면 환경이 바뀔 때마다 코드를 고쳐야 하지만, 환경변수로 분리하면 코드는 그대로 두고 배포 환경별로 값만 다르게 주입할 수 있다 (`.env` vs Render 대시보드의 Environment Variables).

**키 관리**
`GEMINI_API_KEY`나 Firebase 서비스 계정 키가 GitHub 같은 공개 저장소에 노출되면, 제3자가 이 키로 내 계정의 API를 무단으로 호출해 과금이 발생하거나 내 Firestore 데이터에 마음대로 접근할 수 있다. 그래서 이 프로젝트는:
- 로컬: `.env` 파일에 키를 두고 `.gitignore`로 커밋 대상에서 제외
- 배포: Render/Vercel 대시보드의 환경변수 설정 기능에 직접 등록 (코드나 저장소에는 값이 전혀 남지 않음)

이 두 원칙(코드와 키의 분리, `.gitignore`)을 지켰기 때문에 실제로 이번 프로젝트에서 키가 GitHub에 노출된 적이 없다.
